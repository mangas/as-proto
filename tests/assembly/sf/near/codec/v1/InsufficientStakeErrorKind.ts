// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v4.24.4

import { Writer, Reader } from "as-proto/assembly";
import { BigInt } from "./BigInt";

export class InsufficientStakeErrorKind {
  static encode(message: InsufficientStakeErrorKind, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.accountId);

    const stake = message.stake;
    if (stake !== null) {
      writer.uint32(18);
      writer.fork();
      BigInt.encode(stake, writer);
      writer.ldelim();
    }

    const minimumStake = message.minimumStake;
    if (minimumStake !== null) {
      writer.uint32(26);
      writer.fork();
      BigInt.encode(minimumStake, writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): InsufficientStakeErrorKind {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new InsufficientStakeErrorKind();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.accountId = reader.string();
          break;

        case 2:
          message.stake = BigInt.decode(reader, reader.uint32());
          break;

        case 3:
          message.minimumStake = BigInt.decode(reader, reader.uint32());
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  accountId: string;
  stake: BigInt | null;
  minimumStake: BigInt | null;

  constructor(
    accountId: string = "",
    stake: BigInt | null = null,
    minimumStake: BigInt | null = null
  ) {
    this.accountId = accountId;
    this.stake = stake;
    this.minimumStake = minimumStake;
  }
}
